# show a list of all users on sys
# [Cut Cmd] Print selected fields from each input FILE to standard output
# [Syntax] cut -f list [-s] [-d delim] [file ...]

cut -d: -f1 /etc/passwd

# Create password
echo -n @ && cat /dev/urandom | env LC_CTYPE=C tr -dc [:alnum:] | head -c 15 && echo
